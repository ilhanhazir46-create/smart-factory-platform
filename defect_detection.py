import tensorflow as tf\nfrom tensorflow.keras.models import load_model\nimport numpy as np\nfrom tensorflow.keras.preprocessing import image\n\n# Load pre-trained model for object recognition\nmodel = load_model('object_recognition_model.h5')\n\ndef classify_defect(img_path):\n    # Load and preprocess image\n    img = image.load_img(img_path, target_size=(224, 224))\n    img_array = image.img_to_array(img)\n    img_array = np.expand_dims(img_array, axis=0)\n    img_array /= 255.0  # Normalize to [0, 1]\n\n    # Predict object\n    predictions = model.predict(img_array)\n    class_index = np.argmax(predictions)\n\n    return class_index\n\nif __name__ == '__main__':\n    img_path = 'image_to_test.jpg'  # Update with the path to the test image\n    result = classify_defect(img_path)\n    print(f'Detected class index: {result}')